{
  "functions": {
    "source": "./dist",
    "predeploy": [
      "npm run lint",
			"npm run build",
			"npm run copy-deps",
			"npm run install-deps"
    ]
  },
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  }
}
